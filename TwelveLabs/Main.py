from YTSDownloader import Downloader

if __name__ == "__main__":
		yt_dlp = Downloader()
		yt_dlp.download()
	##embedder = Embedder(api_key=os.getenv("TWELVELABS_APIKEY"), input_dir="shorts")
	##embedder.embed_all()
